fastapi==0.115.0
# Plain uvicorn, not [standard]: that extra pulls uvloop, httptools,
# websockets, watchfiles and PyYAML (~35MB) for throughput this app never
# needs -- a request here spends ~12s waiting on an LLM. The one piece that
# is useful locally, watchfiles for --reload, lives in requirements-dev.txt.
uvicorn==0.32.0
pydantic==2.9.2
python-docx==1.1.2
pypdf==5.0.1
pdfplumber==0.11.4
weasyprint==62.3
pydyf<0.11
jinja2==3.1.4
openai==1.51.0
# openai==1.51.0's AzureOpenAI client still passes the `proxies` kwarg to
# httpx internally, which httpx>=0.28 removed -- pin below that until the
# openai SDK is upgraded.
httpx<0.28
python-multipart==0.0.12
slowapi==0.1.9
pytesseract==0.3.13
Pillow==12.3.0
# Renders the project's own README/OVERVIEW for the in-app docs viewer
# (src/api/project_docs.py) -- not used for anything user-supplied.
markdown==3.7
