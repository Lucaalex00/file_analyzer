[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: hits the real AI provider (costs calls, non-deterministic) -- run with `make eval`
# The live eval is opt-in: the default suite stays fast, free and
# deterministic, which is what CI needs.
addopts = -m "not live"
