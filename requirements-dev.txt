-r requirements.txt
# Powers uvicorn --reload (docker-compose.yml); without it uvicorn falls
# back to polling the filesystem. Not worth shipping to production.
watchfiles==0.24.0
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
ruff==0.6.9
